author: Stephen Monroe (VioVioCity)
main: com.vioviocity.slammer.Slammer
name: Slammer
version: 0.2.0
commands:
  slam:
    description:
    usage: /<command> (player) (jail), /<command> [list], /<command> [set|del] (slammer)
  free:
    description:
    usage: /<command> (player)
permissions:
  slammer.slam:
    description: Allows use of /slam (player) (slammer), also unjail (player)
    default: op
  slammer.list:
    description: Allows use of /slam [list].
    default: op
  slammer.set:
    description: Allows use of /slam [set|del] (slammer)
    default: op